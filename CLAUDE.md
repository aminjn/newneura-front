# Neura — روال تحویل (همیشه رعایت شود)

پس از هر تغییری در کد، در پایان هر پاسخ:

1. **index.html** — به‌صورت خودکار از سورس زنده (`app.html` → fetch مستقیم `src/`) به‌روز می‌شود. کافی است فایل‌های `src/**/*.tsx` ویرایش شوند. (نیازی به ساخت جدا نیست، فقط مطمئن شو ویرایش روی سورس واقعی انجام شده.)

2. **neura-offline.html** — باندل standalone را آپدیت کن:
   - فایل تغییرکرده را با `lib/babel.min.js` ترنسپایل کن.
   - `window.__APP_GZ__` را داخل `neura-offline.html` با نسخه‌ی gz جدید پچ کن.
   - `vfs-text.js` و `src-vfs/text.json` را هم سینک کن.
   - اگر نوشتن فایل ۱۰مگابایتی stall شد، چند ثانیه صبر کن و دوباره تلاش کن.

3. **لینک عمومی** — همیشه در انتهای پاسخ `get_public_file_url` روی `neura-offline.html` را بگیر و لینک به‌روز را بده (این فایلِ standalone روی موبایل کار می‌کند؛ index.html فقط برای پیش‌نمایش داخل ادیتور است).

نکته: ویرایش‌ها روی سورس واقعی `src/` انجام شود تا هر دو خروجی هم‌زمان به‌روز بمانند.
